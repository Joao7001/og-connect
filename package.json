{
  "name": "grupo-plus-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/*"],
  "scripts": { "dev:web": "npm run dev --workspace=criadores-web", "dev:api": "npm run dev --workspace=criadores-api", "build": "npm run build --workspace=criadores-web && npm run build --workspace=criadores-api" }
}
